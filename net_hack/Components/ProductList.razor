@* 
<div class="card-columns">

    @foreach (var product in Model.Products)
    {
        <div class="card">
            <div class="card-img" style="background-image: url('@product.Image');"></div>
            <div class="card-body">
                <h5 class="card-title"> @product.Title</h5>
            </div>
        </div>
        <div class="card-footer">
        <small class="text-muted">
            <button @onclick="e=>SelectProduct(product.Id))" data-toggle="model" data-target="#productModal" class="btn btn-primary" >More-Indfo</button>
        </small>
        </div>

    }
</div>

@code{
Product selectProduct;
string selectProductId;
void SelectProduct(string productId){
selectProduct = productId;
selectProduct = ProductService.GetProducts().First(x=>x.Id == productId);
}

} *@